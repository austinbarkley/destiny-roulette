import { DialogContent, DialogTitle } from "@mui/material";

const Spinning = () => {
  return (
    <>
      {/* TODO: Make this a cool animation */}
      <DialogTitle>Running the numbers</DialogTitle>
      <DialogContent>IS SPINNING</DialogContent>
    </>
  );
};

Spinning.propTypes = {};

export default Spinning;
